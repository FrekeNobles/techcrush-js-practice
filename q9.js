// Looping

function fiveMultiplication() {
    for (let i = 1; i <= 10; i++) {
        console.log(`5 x ${i} = ${5 * i}`); // used templat literal to "show working" for the multiplication
    }
}

fiveMultiplication();